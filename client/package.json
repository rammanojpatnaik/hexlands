{
  "name": "hexlands-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "postinstall": "dotnet tool restore",
    "dev": "dotnet fable watch src -o build --run vite",
    "build": "dotnet fable src -o build --run vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
